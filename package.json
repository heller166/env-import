{
  "name": "env-import",
  "version": "0.1.2",
  "description": "Import environment variables with dotenv in an absolute import using ES6 import syntax",
  "main": "./distribution/index.js",
  "scripts": {
    "build": "babel source --presets babel-preset-es2015 --out-dir distribution",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heller166/env-import.git"
  },
  "author": "Juan Francisco Alfaro",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/heller166/env-import/issues"
  },
  "homepage": "https://github.com/heller166/env-import#readme",
  "dependencies": {
    "dotenv": "^4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
